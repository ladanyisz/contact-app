export enum ButtonType {
    Primary = "primary",
    Secondary = "secondary",
    Special = "special"
}

export enum ButtonVariation {
    IconAndText = "iconAndText",
    Text = "text",
    Icon = "icon"
}